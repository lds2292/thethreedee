[build]
  base    = "3d-viewer"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.production]
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- ."
