# Worklog — 2026-03-16

## 작업 내용

### 1. 404 페이지 구현
- `src/pages/NotFoundPage.vue` 생성
  - 그라디언트 "404" 텍스트, "페이지를 찾을 수 없습니다" 타이틀
  - "뷰어로 돌아가기" 버튼 (`href="/"`)
  - 다크 테마 일관성 유지
- `App.vue`: `window.location.pathname !== '/'` 이면 NotFoundPage 표시
- `public/_redirects` 생성: Netlify SPA 라우팅 (`/* /index.html 200`)
  - 없으면 Netlify가 직접 404를 반환해 Vue가 실행되지 않음

### 2. PC에서 창 축소 시 MobileBlock 노출 버그 수정
- **원인**: `App.vue`에서 `window.resize` 이벤트마다 `isMobile`을 업데이트
- **수정**: 페이지 로드 시 한 번만 체크, 이후 resize에서 업데이트하지 않음
- **효과**: PC에서 창을 좁혀도 뷰어가 유지됨

### 3. 창 축소 후 확장 시 뷰어 높이 미복원 버그 수정
- **원인**: isMobile이 flip되면 ModelViewer가 unmount/remount되고, 이때 CSS 레이아웃 확정 전에 `initThree()`가 잘못된 크기를 읽음
- **수정**: 위 2번 수정과 동일 (isMobile을 freeze하면 viewer가 항상 마운트 상태 유지)

### 4. 광고 우측 사이드바로 이동
- **기존**: 하단 footer (`height: 100px`, Leaderboard 728×90)
- **변경**: 우측 300px 고정 사이드바
  - `data-ad-format="auto"` → AdSense가 Half Page(300×600) 또는 Medium Rectangle(300×250) 자동 선택
  - 단가가 높은 포맷으로 수익 개선 기대
- `App.vue`: `.main`을 `flex-direction: row`로 변경, `.content-area` + `.ad-sidebar` 구조
- `DropZone.vue`: 중복 광고 유닛 및 `adsbygoogle.push()` 제거

## 커밋 목록
- `bcabaa4` feat: add 404 not-found page with Netlify SPA redirect
- `97c4544` fix: MobileBlock and viewer resize by freezing isMobile on load
- `1b0dea2` feat: replace bottom ad footer with 300px right sidebar
